<!--pages/nav/nav.wxml-->
<view class="nav">
  <view class="nav-item" wx:for="{{navList}}" bindtap="switchNav"data-survey="{{item.isSurvey}}" data-url="{{item.url}}">
    {{item.name}}
  </view>
  <view class="tips hide" bindtap="toDetail">
    <text>{{tips}}</text>
  </view>
</view>
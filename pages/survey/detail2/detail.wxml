<!--pages/survey/detail2/detail.wxml-->
<questionnaire bind:answerQuestion="answerQuestion" list="{{surveyZero}}" />
<view class="submit-box" wx:if="{{typeName!==null}}">
    <button bindtap='submit'>提交</button>
  </view>
